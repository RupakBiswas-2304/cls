import setuptools


from setuptools import setup

setup(
    name = "cls",
    version="0.1",
    description="Clear the screen, Usefull while working with the interpreter",
    author="Rupak Biswas",
    packages=['cls'],
    install_requires=[],
)